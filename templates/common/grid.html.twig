{% extends '@PrezentGrid/Grid/grid.html.twig' %}

{%- block grid_action -%}
    {% if icon is defined %}
        {% set content %}
            <i class="{{ icon }}"></i>
        {% endset %}
    {% endif %}
    {{ parent() }}
{%- endblock -%}

{%- block grid_rsvp_widget -%}
    <i class="fas fa-{{ value }} rsvp-{{ value }}"></i>
{% endblock %}
