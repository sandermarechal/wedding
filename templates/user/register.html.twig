{% extends 'page.html.twig' %}

{% block content %}
    {{ form_start(form) }}
        {{ form_row(form.name) }}
        {{ form_row(form.email) }}
        {{ form_row(form.plainPassword) }}
        <input type="submit" class="button" value="{{ 'form.user.submit'|trans }}" />
    {{ form_end(form) }}
{% endblock %}
